export const TIME_ZONE_IDENTIFIER: string = "America/Bogota";
export const TIME_ZONE_OFFSET_MINUTES: number = -5 * 60;

export const MINUTES_PER_HOUR: number = 60;
export const MILLISECONDS_PER_MINUTE: number = 60 * 1000;
export const MILLISECONDS_PER_HOUR: number = MINUTES_PER_HOUR * MILLISECONDS_PER_MINUTE;
export const MILLISECONDS_PER_DAY: number = 24 * MILLISECONDS_PER_HOUR;

export const WORK_START_MINUTES: number = 8 * MINUTES_PER_HOUR;
export const LUNCH_START_MINUTES: number = 12 * MINUTES_PER_HOUR;
export const LUNCH_END_MINUTES: number = 13 * MINUTES_PER_HOUR;
export const WORK_END_MINUTES: number = 17 * MINUTES_PER_HOUR;

export const HOLIDAY_CACHE_YEARS_FORWARD: number = 5;
export const HOLIDAY_CACHE_YEARS_BACKWARD: number = 1;
